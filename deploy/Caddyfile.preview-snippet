# Caddy-in-Docker preview route for the WeFetePass side-by-side build.
# The preview container must be attached to the same Docker network as Caddy.

preview.wefetepass.com {
    encode zstd gzip
    reverse_proxy wefetepass-preview:3000
}
